local argparse = require("argparse")
local inspect = require("inspect")
local cjson = require("cjson")

return {
    name = 'luxdummy2',
    _VERSION = "0.1.0",
    argparse = argparse,
    inspect = inspect,
    cjson = cjson,
}
